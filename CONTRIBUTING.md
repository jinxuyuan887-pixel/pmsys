# PMSYS 两人协作规范

本仓库由 `jinxuyuan887-pixel` 与 `lanlan940922-ops` 共同维护。目标是确保两人可以并行开发，同时保证线上版本始终可追溯、可审核、可回退。

## 1. 仓库与分支

- `jinxuyuan887-pixel/pmsys` 是唯一主仓库。
- `main` 是唯一线上发布基线，不在 `main` 直接开发，不强制推送。
- 开始工作前先同步 `main`，再创建独立分支。
- 金旭媛使用 `jin/feature-简述`、`jin/fix-简述` 等分支。
- lanlan 使用 `lanlan/feature-简述`、`lanlan/fix-简述` 等分支。
- 紧急线上修复使用 `<姓名>/hotfix-简述`，仍需通过 Pull Request。

示例：

```bash
git switch main
git pull --ff-only origin main
git switch -c jin/feature-task-management
```

## 2. 提交要求

- 一次提交只解决一个明确问题，避免把无关文件混在一起。
- 提交信息使用 `类型: 简述`，例如 `feat: 增加任务与服务记录关联`。
- 推荐类型：`feat`、`fix`、`docs`、`refactor`、`test`、`chore`。
- 不提交密码、密钥、线上数据库、上传附件、`.env`、构建产物或本地缓存。
- 仓库统一使用 npm 和 `package-lock.json`，不提交 pnpm 锁文件。

## 3. Pull Request 与审核

- 所有代码通过 Pull Request 合并到 `main`。
- PR 应说明改了什么、为什么改、影响范围、如何验证，以及是否包含数据库迁移。
- 发起人先完成自检，再请求另一位贡献者审核。
- 审核人重点检查数据兼容性、权限、金额计算、外部链接、删除操作和部署影响。
- 自动检查通过、审核意见处理完毕后再合并。
- 默认使用 Squash merge，让 `main` 每个功能只保留一个清晰提交。

两人同时修改同一功能时，先在 Issue 或群内明确文件负责人；后开始的人基于对方最新分支继续，避免分别重写同一文件。

## 4. 数据库迁移

- 已经进入 `main` 的迁移文件不得修改、重命名或删除。
- 每次结构变化新增一个递增迁移文件，并同步 `db/schema.ts` 与 `drizzle/meta/_journal.json`。
- 迁移必须兼容已有线上数据，不在迁移中清空真实业务数据。
- 发布前先备份线上数据库，再执行迁移，再启动新版本。
- 破坏性字段变更采用“新增字段 → 迁移数据 → 稳定后删除旧字段”的分阶段方式。

## 5. 合并前检查

```bash
npm ci
npm run lint
npm run build
```

涉及核心业务时还需检查：

- 登录与权限是否正常。
- 项目、任务、服务记录的增删改查是否正常。
- 外部填写、审核、附件与复制链接是否正常。
- 金额、成本、利润率和交付进度是否正确。
- 数据库迁移前后记录数量是否一致。

## 6. 发布与回退

- 只有合并后的 `main` 可以发布到阿里云。
- 发布提交的 SHA、发布时间、迁移文件和执行人应写入 PR 或发布记录。
- 禁止从个人未合并分支直接覆盖线上。
- 发布失败时优先回退到上一个已验证镜像；数据库按发布前备份恢复，禁止临时手改线上数据掩盖问题。

## 7. 推荐 GitHub 设置

为 `main` 开启分支保护：

- 必须通过 Pull Request 才能合并。
- 至少 1 人审核通过。
- 新提交后撤销旧审核结果。
- 必须通过 `quality` 自动检查。
- 合并前分支必须为最新。
- 禁止强制推送和删除分支。
- 管理员也遵守以上规则。
